# yaml-language-server: $schema=https://goreleaser.com/static/schema.json
version: 2

project_name: geckos3

before:
  hooks:
    - go mod tidy
    - go test -count=1 ./...

builds:
  - id: geckos3
    binary: geckos3
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
      - arm
    goarm:
      - "7"
    ignore:
      - goos: windows
        goarch: arm
      - goos: windows
        goarch: arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.date={{.Date}}
    flags:
      - -trimpath

archives:
  - id: geckos3
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    files:
      - README.md
      - LICENSE*

dockers:
  - image_templates:
      - "randiltharusha/geckos3:{{ .Version }}"
      - "randiltharusha/geckos3:latest"
    dockerfile: Dockerfile.goreleaser
    build_flag_templates:
      - "--pull"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.source={{.GitURL}}"

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  use: github
  format: "{{.Message}}"
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"
      - Merge pull request
      - Merge branch
  groups:
    - title: Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: "Bug Fixes"
      regexp: '^.*?fix(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: "Security Updates"
      regexp: '^.*?sec(\([[:word:]]+\))??!?:.+$'
      order: 2
    - title: Others
      order: 999

release:
  github:
    owner: randilt
    name: geckos3
  draft: false
  prerelease: auto
  mode: append
  header: |
    ## geckos3 {{ .Tag }} ({{ .Date }})

    Lightweight S3-compatible object storage server

    ### Installation

    **Docker (Recommended):**
    ```bash
    docker pull randiltharusha/geckos3:{{ .Tag }}
    # or
    docker pull randiltharusha/geckos3:latest
    ```

    **Binary:**
    Download the archive for your platform below, extract it, and run:
    ```bash
    ./geckos3
    ```
  footer: |
    **Full Changelog**: https://github.com/randilt/geckos3/compare/{{ .PreviousTag }}...{{ .Tag }}

    ---

    Released with [GoReleaser](https://goreleaser.com)
